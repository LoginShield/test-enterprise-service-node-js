// var expect  = require('chai').expect;
// const ajax = require('axios');
// const pkg = require('../../package.json');

describe('website', function(){
    /*
    it('front-page', async function(){
        this.timeout(5000);
        const response = await ajax.get('http://localhost:'+process.env.PORT+'/version', {headers: {'Accept':'application/json'}});
        expect(typeof response).to.equal('object');
        expect(response.data.name).to.equal(pkg.name);
        expect(response.data.version).to.equal(pkg.version);
    })
    */
})